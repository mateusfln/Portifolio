create function mini (valor_1 numeric, valor_2 numeric)returns numeric as $$
declare
mini numeric;
begin
mini = least(valor_1, valor_2);
return mini;
end; $$
language plpgsql;

select * from clientes
SELECT avg(cod) from clientes
select * from estoque
SELECT avg(qtd) from estoque
SELECT concat ('O cliente ', nome, ' possui o email :', email) as "Email"
FROM clientes;
SELECT concat ('O cliente ', nome, ' possui o numero : ', telefone) as "Telefone"
FROM clientes;
SELECT MIN(cod) FROM estoque;
select cpf, cod from clientes
select min(qtd)

select mini (890, 87)
